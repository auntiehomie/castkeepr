import { HTMLAttributes } from 'react';
export interface UserSuggestion {
    username: string;
    pfp_url?: string;
    display_name: string;
    viewer_context?: {
        following?: boolean;
        followed_by?: boolean;
        blocking?: boolean;
        blocked_by?: boolean;
    };
}
interface ISuggestiveInputProps extends HTMLAttributes<HTMLInputElement> {
    width?: string;
    height?: string;
    placeholder?: string;
    selectedUsers: UserSuggestion[];
    onSelectUsers: React.Dispatch<React.SetStateAction<never[]>>;
}
export declare const NeynarAutoCompleteUser: React.FC<ISuggestiveInputProps>;
export {};
